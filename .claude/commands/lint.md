Run the design system lint check and report all violations.

```bash
bash /Users/zinklee/Code/timer-app-for-my-gf/scripts/lint_design.sh 2>&1
```

For each violation, identify the file, line, and what rule is broken (from docs/DESIGN_RULES.md). Fix all violations unless they are documented exceptions.
